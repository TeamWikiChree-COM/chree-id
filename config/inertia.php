<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Pages
    |--------------------------------------------------------------------------
    |
    | ページの置き場。既定は resources/js/pages だが、このプロジェクトは
    | Pages と大文字で始めている。Windows では区別が付かず気付けないので、
    | ここで明示しておく (本番と CI は Linux で、既定のままだと必ず外れる)。
    |
    */

    'pages' => [

        'paths' => [

            resource_path('js/Pages'),

        ],

        // 設定ファイルは深くマージされないので、既定値も書き出しておく
        'ensure_pages_exist' => false,

        'extensions' => [

            'js',
            'jsx',
            'svelte',
            'ts',
            'tsx',
            'vue',

        ],

    ],

];
