# git 追跡外でも、デプロイのたびに送るパス。1 行 1 件。末尾が / ならディレクトリ以下すべて。
#
# ビルド成果物をリポジトリに置きたくない一方で、CORESERVER では npm を回せないため
# サーバ側でビルドできない。CI がデプロイ直前に npm run build するので、
# その成果物をここから直接送る。
#
# 差分計算は git の履歴を見るので追跡外のファイルは差分に現れない。
# ここに挙げたものは毎回まるごと送られる。
public/build/

# 翻訳。正は lang/*.json で、読むのは CI が生成した PHP (ARCHITECTURE.md 10章)
generated/lang/
